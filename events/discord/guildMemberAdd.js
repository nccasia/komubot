require("../../util/extenders.js");

module.exports = {
    async execute(e, client) {
        // try {
        //     if (e.author != null) {
        //         e.author.addDB();
        //     }
        // } catch(err) {
        //     console.log(err);
        // }
    }
};